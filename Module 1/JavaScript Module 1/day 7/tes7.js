   
   let obj1 = {
    nama: 'a',
    umur: 20
   };
   
   let obj2 = {
    nama: 'a',
    umur: 30,
   };

   function pembanding (obj1, obj2) {
    let arr1 = Object.keys(obj1);
    let arr2 = Object.keys(obj2)
   
   if (arr1.length !== arr2.length){
    return false
   }
   for (i=0; i < arr1.length; i++) {
    if ()
   }
}